#include <stdio.h> // standard input output header file

int main(){
    printf("Hello World!\n"); // Hello World!
    return 0;
}

/*
Compile: gcc hello.c -o hello.exe (Windows) or gcc hello.c -o hello (Linux)
Run: .\hello.exe (Windows) or ./hello.exe (Linux)

or

Compile: g++ hello.c -o hello.exe (Windows) or g++ hello.c -o hello (Linux)
Run: .\hello.exe (Windows) or ./hello.exe (Linux)

or

Compile: g++ hello.c
Run: ./a.out (Linux) or .\a.exe (Windows)
*/